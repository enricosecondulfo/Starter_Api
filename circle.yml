
dependencies:
  cache_directories:
    - mongodb-linux-x86_64-3.2.6.tgz

  pre:
    - bash ./scripts/ci/install_dependencies.sh

  override:
    - java -version
    - echo $GRADLE_HOME
    - gradle -version

